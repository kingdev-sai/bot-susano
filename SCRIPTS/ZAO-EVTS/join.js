module.exports.config = {
  name: "welcome",
  eventType: ["log:subscribe"],
  version: "1.0.0",
  credits: "GryKJ",
  description: "حدث ترحيب — معطّل (يُستخدم 0joinNoti_0 بدلاً منه)"
};

module.exports.run = async function ({ event, api }) {
};
